export enum registerAttribute {
  "login" = "login",
  "email" = "email",
  "password" = "password",
}

class Myregister extends HTMLElement {
  login?: string;
  email?: string;
  password?: string;
}
